<ul id="sub_nav" class="admin-subnav" data-hook="admin_user_sub_tabs">
  <% if can? :admin, Spree::User %>
    <%= tab :users %>
  <% end %>
  <% if can? :admin, Spree::Role %>
    <%= tab :roles %>
  <% end %>
</ul>
